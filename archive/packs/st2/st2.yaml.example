---
base_url: ""
api_url: ""
auth_url: ""

# API key used to authenticate against st2.
# Note: This only needs to be specified if you want to use a custom API key.
# By default, this pack will use temporary authentication token which is
# generated by the runner and is available to the action during execution.
api_key: ""

# If this setting is provided, server certificate validation is performed.
# By default, client assumes self-signed cert and doesn't perform certificate
# validation.
# This value can either contain a path to the CA_BUNDLE file with certificates
# of trusted CAs or "True" to use CAs from the system trust store. If not
# provided, no validation is performed.
cacert: ""
