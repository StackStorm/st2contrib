---
name: list_private_messages
runner_type: python-script
description: List all private messages for the authenticated user
enabled: true
entry_point: list_private_messages.py
parameters:
  older_than_message:
    type: integer
    description: Only show messages older than this message ID
    required: false
  newer_than_message:
    type: integer
    description: Only show messages newer than this message ID
    required: false
  limit:
    type: integer
    description: Limit the number of messages
    required: false
