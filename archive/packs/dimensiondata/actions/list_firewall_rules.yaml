---
name: list_firewall_rules
runner_type: python-script
description: List the firewall rules in a network domain
enabled: true
entry_point: run_compute_network_domain.py
parameters:
  action:
    type: string
    immutable: true
    default: ex_list_firewall_rules
  region:
    type: string
    description: The region code
    default: dd-na
  network_domain_id:
    type: string
    description: The ID of the network domain