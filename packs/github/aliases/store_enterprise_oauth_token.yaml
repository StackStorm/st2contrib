---
name: "store_enterprise_oauth_token"
pack: "github"
description: ""
action_ref: "github.store_enterprise_oauth_token"
formats:
  - display: "github store enterprise token {{token}}"
    representation:
      - "github store enterprise token {{token}}"
ack:
  enabled: false
  append_url: false
result:
  format: |
    {% if execution.status == 'succeeded' %}
    Your GitHub enterprise token has been stored!
    {% else %}
    Error: {{execution.result.stdout}}{~}See {{execution.id}} for more details.
    {% endif %}
