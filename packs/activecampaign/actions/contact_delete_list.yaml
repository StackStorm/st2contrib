description: Delete multiple existing contacts.
enabled: true
entry_point: run.py
name: contact_delete_list
parameters:
  api_action:
    default: contact_delete_list
    description: contact_delete_list
    required: false
    type: string
  api_key:
    description: Your API key
    required: false
    type: string
  api_output:
    default: json
    description: xml, json, or serialize (default is XML)
    required: false
    type: string
  ids:
    description: ID's (comma-separated) of the contacts you wish to delete
    required: true
    type: string
runner_type: run-python
