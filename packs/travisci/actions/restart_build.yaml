name: restart_build
runner_type: run-python
description: Restart a Build
enabled: true
entry_point: restart_build.py
parameters:
  buildid:
    type: integer
    description: id of the build to perform action on
    required: true
