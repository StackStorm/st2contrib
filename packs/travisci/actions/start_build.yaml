name: start_build
runner_type: run-python
description: Restart or Cancel a Build
enabled: true
entry_point: start_build.py
parameters:
  buildid:
    type: integer
    description: id of the build to perform action on
    required: true
  action:
    type: string
    description: action to perform on last build, restart or cancel
    required: true
