name: create_service
runner_type: run-python
description: Create a service/hook for a given repository
enabled: true
entry_point: create_service.py
parameters:
  repo:
    type: string
    description: provide repositroy name for listing the branches
    required: true
  url:
    type: string
    description: provide repositroy name for listing the branches
    required: true
  service:
    type: string
    description: service to hook, for example, POST, issues, Jenkins, ReadtheDocs etc
    required: true
