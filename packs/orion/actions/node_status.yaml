---
description: "Query Solarwinds Orion for a node's status (i.e. Up/Down)"
enabled: true
entry_point: 'status.py'
name: "status"
pack: "orion"
runner_type: "python-script"

parameters:
  node:
    type: "string"
    description: "The node to query for state in the monitoring platform."
    required: true
  platform:
    type: "string"
    description: "The Orion Platform to act on."
    required: true
