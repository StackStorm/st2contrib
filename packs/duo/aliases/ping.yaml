---
name: "ping"
pack: "duo"
description: "Check the Duo platform can be reached."
action_ref: "duo.ping"
formations:
  - "duo ping"
ack:
  enabled: true
  append_url: false
  format: "Cheking connection to Duo..."
result:
  formaat: |
    {% if execution.status == 'succeeded' %}
    Pong! Succeeded{~}
    {% else %}
    Failed!{~}
    {% endif %}

