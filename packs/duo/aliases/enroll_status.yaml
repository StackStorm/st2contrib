---
name: "enroll_status"
pack: "duo"
description: "Check your enroll status via the Duo platform"
action_ref: "duo.auth"
formations:
  - "duo enroll status"
ack:
  enabled: true
  append_url: false
  format: "Checking if your enrolled with Duo"
result:
  formaat: |
    {% if execution.status == 'succeeded' %}
    Your enrolled!{~}
    {% else %}
    Your not enrolled!{~}
    {% endif %}

