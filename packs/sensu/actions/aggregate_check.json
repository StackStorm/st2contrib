{
    "name": "sensu_check_aggregates",
    "runner_type": "run-local",
    "description": "Get Sensu check aggregates",
    "enabled": true,
    "entry_point": "aggregates.py",
    "parameters": {
        "check": {
            "type": "string",
            "description": "The client name"
        },
        "age": {
            "type": "string",
            "description": "The number of seconds old an aggregate must be to be listed."
        }
    },
    "required_parameters": ["check"]
}
