---
name: unsilence
runner_type: run-local-script
description: Unsilence a Sensu client or check
enabled: true
entry_point: unsilence.py
parameters:
  check:
    type: string
    description: The check to unsilence
  client:
    type: string
    description: The client to unsilence
    required: true
  sudo:
    immutable: true
  kwarg_op:
    immutable: true
