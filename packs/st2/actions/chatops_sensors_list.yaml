---
name: "chatops.sensors.list"
enabled: true
description: "Retrieve a list of available StackStorm sensors and format output for ChatOps consumption."
runner_type: "action-chain"
entry_point: "workflows/chatops/sensors_list.yaml"
parameters:
  skip_notify:
    default:
      - "retrieve_sensors_list"
  pack:
    type: "string"
    description: "Optional pack to filter on"
    required: false
