---
name: "chatops.format_list_result"
enabled: true
description: "Format result of an action which returns a list of dicts so it can be consumed via ChatOps."
runner_type: "run-python"
entry_point: "chatops_format_list_result.py"
parameters:
  result:
    type: "array"
    description: "Result to format."
  attributes:
    type: "array"
    description: "Which attributes of each result item to include in the formatted result."
